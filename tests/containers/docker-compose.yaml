version: '3.6'

services:
  tester:
    build:
      context: ../..
      dockerfile: Dockerfile
    networks:
      - kind
    command: pytest ./tests/integration
    environment:
      KUBE_URL: 0.0.0.0
      KUBE_TOKEN: $KUBE_TOKEN
      KUBE_NAMESPACE: default

networks:
  kind:
    external: true